# TODO Audit Findings — 2026-06-06

## How to read this
Each row below is a verified opportunity: it lists the effort, the risk, whether it changes what people see in Discord (live-visible), how confident the auditor was, and the exact file:line where the proof lives.

## Ranked shortlist (top ~12 by value-to-effort)

| # | Opportunity | Kind | Effort | Risk | Live-visible | Confidence | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | Block fake/penny tickers on `!all` (reuse existing market-cap check) | build-new | S | low | yes | 0.82 | aggregator.py:1223; tickers.py:144 |
| 2 | Cut the `!all` cleanup phase — it's the latency hog, proven to not change output | optimize-cost | S | low | yes | 0.80 | narrator.py:287-289; todo/all-command-quality.md:179 |
| 3 | Route options-flow alerts to their own channel (stop flooding main, ~175/day) | optimize-quality | S | low | yes | 0.82 | main.py:372; main.py:874-877 |
| 4 | Surface chart-pattern strength in `!all` (already computed, thrown into prose only) | build-new | S | low | yes | 0.72 | patterns.py:182; embed.py:708-746 |
| 5 | Data-sparseness banner on thin tickers in `!all` | build-new | S | low | yes | 0.66 | aggregator.py:299-327; embed.py:708-765 |
| 6 | Skip the wasted 15s groq head-start on big tickers that always 413-reject | optimize-cost | S | low | yes | 0.70 | llm_client.py:270-273; todo/all-command-quality.md:183 |
| 7 | Disable Gemini "thinking" + cap output on Wolf chart reads (stretch scarce free quota) | optimize-cost | S | low | no | 0.72 | wolf_vision.py:158-165 |
| 8 | Fix root cause: every `!all` falls to generic ATR levels — anchor pipeline never lands | optimize-quality | M | med | yes | 0.78 | consensus_engine.log (12/12); levels.py:445-449 |
| 9 | Direction-aware YouTube score (stop adding long points for a bearish YouTube consensus) | optimize-quality | M | med | yes | 0.85 | cross_reference.py:230,249,333,364 |
| 10 | Build the SHORT side of Wolf beneficiary inference (v1.1) | build-new | M | med | yes | 0.90 | wolf_beneficiaries.py:104-110; consensus.yaml:818 |
| 11 | Surface "Wolf + N sources agree" inside `!all` responses | build-new | M | low | yes | 0.85 | aggregator.py:179-296; wolf_news.py:58 |
| 12 | Guard Wolf trade-idea entry/target ordering (short must sell high, long must buy low) | bugfix | S | low | yes | 0.82 | wolf_news.py:176,180-181 |

Notes on dedup: the Wolf-vision budget item appears twice in the source data (once as "add a budget bucket", once as "disable thinking + cap output"). They are kept as two separate bullets in the lanes below because they fix different things (one counts/caps the calls, one shrinks each call), but they share the same root file. The `!all` P/C-OI and the head_start latency refactor named in the brief are already shipped and live — they are NOT in this catalog.

## By lane

### Build-new
- **Block fake/penny tickers on `!all`** — `handle_all` only checks ticker format, so `!all FAKEX` or a micro-cap runs the full 30-80s pipeline and returns a confident-looking but empty embed. A `validate_ticker_market_cap` check already exists and is used elsewhere; add one `await` near the top of `handle_all` to reject below the $100M floor. Why better: reuses already-wired code, near-zero new lines, saves 30-80s of wasted compute and stops misleading output. Effort S / risk low. Live-visible: yes. Evidence: aggregator.py:1223 (only format checked), tickers.py:144 (validate exists), consensus.yaml:328 (floor config).
- **Surface chart-pattern strength in `!all`** — the pattern detector already returns a pattern name, a confidence, and a key level every run, but it only feeds the AI prompt; the embed shows no pattern at all. Add a small "Pattern" field (e.g. "Bull flag — breakout watch $130 (0.72)"). Why better: a new user-visible signal at near-zero cost because the data is already computed and discarded into prose. Effort S / risk low. Live-visible: yes. Evidence: patterns.py:175-179,182; aggregator.py:347; narrator.py:911-914; embed.py:708-746.
- **Data-sparseness banner on thin tickers** — the source count per run is already in hand; on obscure tickers it can be 2 or fewer, yet the embed still renders a full confident trade plan with no caveat. Add a one-line banner when sources are few (e.g. "Low coverage — only N sources; levels are ATR-derived."). Why better: stops over-confident output on thin tickers; trivial because the count is already passed to the embed. Effort S / risk low. Live-visible: yes. Evidence: aggregator.py:299-327, aggregator.py:1153, embed.py:708-765.
- **Build the SHORT side of Wolf beneficiary inference (v1.1)** — today a bear macro thesis produces an empty "bot's read" section even though shorts are Wolf's highest-signal calls. Mirror the long-only logic: add short buckets to the universe map, rank the weakest laggards (most-negative move), confirm with bearish catalysts and put-premium dominance, and flip the anti-chase guard. The database and the renderer already support `side='short'` with no change. Why better: completes the feature's stated goal for the bear half of Wolf's calls; build is contained to one analysis file + one yaml + one digest tweak. Effort M / risk med. Live-visible: yes. Evidence: wolf_beneficiaries.py:104-110,229,169-182,42-48; consensus.yaml:818; db.py:738,3291.
- **Surface "Wolf + N sources agree" inside `!all`** — the confluence engine already runs every 15 min and 11 theses currently show cross-source agreement, but `!all` never reads it, so a user running `!all NVDA` never sees Wolf + others are bearish on semis. Add a read-only database lookup that maps the ticker to its thesis and renders one embed line (the renderer already exists). Why better: a unique macro-context signal no other `!all` source provides, with near-zero added latency (one indexed read, no AI call). Effort M / risk low. Live-visible: yes. Evidence: aggregator.py:179-296 (no Wolf read), wolf_news.py:58 (renderer exists), wolf_scope.py:96,150 (mapping exists), live data thesis 22 NDX agree=2 / thesis 10 URA high.
- **Tradier real-time-free options feed (research, needs signup)** — evaluate a free Tradier brokerage account as a fresher options source than yfinance's ~15-min delayed chains. No Tradier/Polygon code exists in the repo — this is greenfield research; signup/approval friction and whether the free tier truly returns real-time data must be confirmed first. Why better: could move the watcher to near-real-time at no recurring cost (the original #18 goal). Effort L / risk med. Live-visible: no. Evidence: grep returns zero matches; todo/options-flow-realtime.md:20.

### Optimize-quality
- **Fix root cause: every `!all` falls to generic ATR levels** — all 12 logged runs (NVDA, TSLA, AMD, MSFT, WEN) show `trade_plan=partial reason="atr_fallback (low anchor confluence)"`. That means the real YouTube/swing/web anchor levels never cluster, so TP1/TP2/TP3 are always synthetic spot±N×ATR and reward:risk is hidden on every run. Loosen the clustering band / confluence requirement so real levels survive, and surface the anchor count so the narrative can be honest when levels are thin. Why better: the single highest-leverage quality change — the headline trade numbers are always placeholders today, and the brief never mentions this (only the logs reveal it). Effort M / risk med. Live-visible: yes. Evidence: consensus_engine.log 12/12 atr_fallback; levels.py:445-449; cluster band 0.005 aggregator.py:786,881; R:R suppression structured_fields.py:117.
- **Direction-aware YouTube score (flag-gated)** — the YouTube boost (5/10/15) is added as a flat positive no matter whether the videos are bullish or bearish, so a YouTuber screaming "short NVDA" raises NVDA's long score. The consensus direction is already computed but thrown away. Behind a default-OFF flag, sign the boost by agreement so a confident bearish consensus correctly pulls a marginal long below the alert line. Why better: a real correctness gap, not just tuning; flag-gated so live alerts stay byte-identical until blessed. Effort M / risk med. Live-visible: yes. Evidence: cross_reference.py:230,249,333,364; structured_fields.py:73.
- **Recency decay on stale YouTube mentions (flag-gated)** — a mention extracted 6.9 days ago counts the same as one from an hour ago, even though the freshness timestamp is already in the query. Behind a default-OFF flag, damp older mentions. Why better: addresses the original lag concern (YouTube trails the move) without touching the base values. Effort M / risk low. Live-visible: yes. Evidence: db.py:1834,1842-1843; cross_reference.py:248-249; todo/youtube_db_score_weighting.md line 21.
- **Channel-reliability weighting of the YouTube boost (flag-gated)** — a `trust_score` per channel already exists and is used to rank YouTube price levels, but the score boost ignores it. Behind a default-OFF flag, scale the boost by the trust of the backing channel. Why better: reuses an already-curated reliability signal the score currently ignores, low new surface. Effort M / risk low. Live-visible: yes. Evidence: db.py:300,1871,1933; cross_reference.py (no trust reference).
- **YouTube-level / technical-level confluence bonus (flag-gated)** — when a YouTube-stated buy zone or target lines up with a computed support/resistance, that is genuine two-source agreement, but the two are never cross-checked. Add a small capped bonus behind a default-OFF flag. Why better: rewards the 2+-independent-source agreement the project's alert philosophy values. Effort L / risk med. Live-visible: yes. Evidence: cross_reference.py:234-245,325 (never compared).
- **Swing horizon uses raw ATR, not realized volatility** — the days-to-target estimate divides by the single 14-day ATR; when recent realized volatility differs (post-earnings crush or a spike) the horizon is off. The daily candles are already in memory, so realized vol is free to compute. Why better: more accurate horizon at no extra API cost; the brief explicitly lists this lever. Effort M / risk low. Live-visible: yes. Evidence: structured_fields.py:319,332; aggregator.py:204-208,1019-1021; todo/all-command-quality.md:76.
- **Options-flow alerts flood the main channel (~175/day)** — every flow alert posts to the same channel as regular signals, and the per-cycle cap of 8 is hit on essentially every cycle, so ~175 options messages/day flood the main channel and any 9th+ qualifying ticker is silently dropped. Route to a dedicated channel or a periodic top-N digest. Why better: directly violates the "quality over quantity" philosophy today; a dedicated channel cuts noise to near zero while losing no data (every hit is already saved for `!all`). Effort S / risk low. Live-visible: yes. Evidence: main.py:372,874-877; database 877 alerts over 5 days, 116 cycles avg 7.6/8.
- **Per-cycle cap (8) silently drops qualifying flow** — on every alerting cycle the cap is hit, so the 8 that fire are just the highest raw-premium names (SPY/QQQ/TSLA dominate), crowding out smaller-cap unusual flow. Make selection smarter than raw premium-sort, or raise the cap. Why better: surfaces the genuinely unusual single-name flow instead of the same mega-cap ETFs every cycle. Effort M / risk med. Live-visible: yes. Evidence: main.py:367-368; options.py:278; database top tickers QQQ/SPY/IWM.
- **Per-ticker "unusual" baseline instead of a flat $250k/5x floor** — a flat dollar floor treats a $250k bet on a small-cap the same as on SPY, so mega-caps trivially clear it every cycle. Add a per-ticker relative baseline (premium vs the ticker's own trailing average) so "unusual" means unusual for that name. The options table already has the history to compute baselines. Why better: raises signal quality and relieves the cap pressure with no new data source. Effort M / risk med. Live-visible: yes. Evidence: consensus.yaml:700-702; database 32,794 baseline rows; db.py:419.
- **Widen the risk-section price gate beyond the stop-loss literal** — the synthesis prompt bans every price level in the Risk section, but the deterministic backstop only catches the stop-loss number, so a leaked entry/target/buy-zone price reaches Discord. Extend the gate to flag any bare "$number" or share-price-magnitude number in that section. Why better: live evidence shows the free model restated banned prices despite the prompt, so the prompt alone is known-unreliable here. Effort M / risk med. Live-visible: yes. Evidence: quality_bar.py:170; narrator.py:650-656; output_filter.py:88-92.
- **Correct the todo's wrong premise on the Wolf-echo filter (likely no-build)** — the deferred echo filter aims to drop source rows that re-quote Wolf, justified by "Twitter rows carry no text" (inaccurate — rows do store text). The real reason to skip it stands: Wolf's newsletter is private/paid, so genuine echoes are ~0 and the 135 "wolf" text hits are noise (Wolfspeed/$WOLF). Recommend closing as WONTFIX with the corrected rationale. Why better: prevents wasted effort and false-positive risk on a non-problem; fixes a factual error in the tracking doc. Effort S / risk low. Live-visible: no. Evidence: todo/wolf-macro-brain.md:54; db.py:932; live probe 1514 twitter rows all have text, 135 noise.
- **Per-Twitter-author vote granularity — feasible but low-value, keep one-net-vote** — the data supports splitting Twitter into per-author votes (handles are stored, 30 distinct in 21 days), but the current one-net-vote-per-source-type is a deliberate anti-crowding design; per-author would let one chatty account tip a thesis to a "critical" ping. Recommend NOT changing the vote unit; at most use distinct-author count as a display color. Why better: documents feasibility while arguing against it on signal-quality grounds, with the exact crowding risk it would reintroduce. Effort M / risk med. Live-visible: yes. Evidence: todo/wolf-macro-brain.md:54; db.py:953; models.py:33; anti-crowding wolf_confluence.py:14-16,146-163,236-238.

### Optimize-cost (token/API savings)
- **Cut the `!all` cleanup (sanitize) phase** — cleanup, not synthesis, dominates big-ticker latency: ~28s of an 86.7s NVDA run was cleanup, and the cleanup chain exhausted 18× vs synthesis 1×. The team's own measurement says cleanup failing has NO visible effect on the writeup. Drop it to a single fast-model attempt (or skip the second model / gate it off on big tickers) to save ~15-28s and ~9 AI calls per `!all` with no output loss. Effort S / risk low. Live-visible: yes. Evidence: narrator.py:287-289,112; todo/all-command-quality.md:179; 18× exhausted in logs.
- **Skip the wasted 15s groq head-start on big tickers** — for source-heavy tickers (NVDA/TSLA, 20 sources) the prompt exceeds groq's free per-minute cap, so groq 413-rejects it — but the code still burns the full 15s groq-solo window before fanning out to the models that actually do the work. Add a cheap prompt-size estimate (chars/4); when it's over ~10k tokens, skip the groq window. Saves up to 15s on exactly the slowest tickers. Effort S / risk low. Live-visible: yes. Evidence: llm_client.py:270-273; todo/all-command-quality.md:183; NVDA/TSLA runs at 86.7s/82.7s.
- **Disable Gemini "thinking" + cap output on Wolf chart reads** — the chart-vision call runs `gemini-flash-latest` with no config, so default reasoning ("thinking") is ON and output is uncapped for what is a fixed-schema JSON extraction. Pass a config that turns thinking off and caps output (~512). Up to 5 reads per email against a tiny ~3-4-call/key/day free quota, so every saved token stretches that scarce quota and cuts 429 rotation. Effort S / risk low. Live-visible: no. Evidence: wolf_vision.py:158-165,35; consensus.yaml:753.
- **Add a wolf_vision budget bucket so chart reads stop starving the video Gemini keys** — the chart-vision path makes Gemini calls with no budget accounting at all, yet shares the exact free-tier key pool and exhaustion tracker the video pipeline depends on (which DOES gate on a daily limit). Add a `wolf_vision_calls` column + config limit + a consume/can-consume gate. Why better: stops uncapped vision calls silently exhausting the shared keys and starving the budgeted video pipeline; makes vision usage visible in the daily table. Effort M / risk low. Live-visible: no. Evidence: wolf_vision.py:139-184; wolf_email_parser.py:357-362; engine.py:55-59,141,152; consensus.yaml:578; todo/wolf-macro-brain.md:32-33.
- **Make the `!all` sanitize LLM phase optional / no-LLM by default** — the config comment itself states the free sanitize pool 429s about half the time and cleanup often falls back to trimmed raw text, with "every test produced a valid narrative regardless." A flag already gates the whole batch; flipping it OFF gives external snippets plain truncation instead of firing up to 7 free-tier calls per `!all` that mostly fail anyway. Why better: eliminates up to 7 calls per command with, per the maintainer's own note, no quality loss — and it's a flag-flip backed by an in-repo note. Effort S / risk med. Live-visible: yes. Evidence: consensus.yaml:278-285; aggregator.py:1071,1083-1089. (Overlaps with the cleanup-phase item above — same phase, two angles.)
- **Drop the two always-empty sanitize batches (searxng + sec)** — the aggregator always passes empty lists for the searxng and sec batches, so those two of the nine concurrent cleanup coroutines can never do real work. Remove them. Why better: shrinks the gather and the surface that can throw/retry; no token win today (they early-return), listed for completeness/clarity. Effort S / risk low. Live-visible: no. Evidence: aggregator.py:1073,1078; narrator.py:197-198,301-312.
- **Cache or trim the ~2,800-token static synthesis instruction block** — the synthesis system instruction plus constraints block is a large, fully static payload sent on every `!all` AND re-sent verbatim on up to 3 retries. Enable prompt caching on the constant prefix, or trim the anti-fabrication prose that repeats the same "cite verbatim / don't invent" rule ~4 times. Why better: input tokens are billed on every call; this is ~2,800+ tokens × (1 + up to 3 retries) on the single most expensive call in the system. Effort M / risk med. Live-visible: no. Evidence: narrator.py:350-372,577-731,1064-1125.
- **Skip the once-per-ticker confidence scorer when the ticker can't reach the alert threshold** — the scoring AI call runs on every ticker that has any technical or catalyst signal, even when its cheap subtotal plus the maximum possible AI boost still can't clear the alert line. Compute the cheap subtotal first and skip the call when it provably can't change the decision. Why better: removes a real AI call (cost + up-to-15s budget) on every low-signal ticker, with zero output loss — the brief itself names this as the safe deferred win. Effort M / risk med. Live-visible: no. Evidence: cross_reference.py:310-319,328-329; todo/speed-accuracy-optimization.md:27.

### Test-gaps
- **No end-to-end test for the concurrent YouTube level-alert path** — the live function that fetches every watched ticker's price concurrently and fires level alerts has no test. Add one that seeds level rows, mocks the price fetch, and asserts an alert fires within the proximity band and that a single per-ticker price error is skipped, not fatal. Why better: a regression in the ticker↔price pairing or the error-skip branch would silently stop or misfire alerts with nothing to catch it. Effort S / risk low. Live-visible: no. Evidence: main.py:896-916; grep of tests finds none.
- **Test the SHORT side end-to-end on a real Wolf bear thesis before going live** — once built, run the short inference against a real active bear thesis and assert the ranked short is a LAGGARD (the sign-flip is correct), then render a digest and confirm the embed. Why better: the long side shipped with a real-data test that caught a direction bug; the short side inverts the riskiest sign logic and must get the same check. Effort S / risk low. Live-visible: no. Evidence: wolf_beneficiaries.py:240-283; project memory phase-4.
- **Prove the live confluence engine has ever fired a real tier-up alert** — the feature was enabled against a seeded thesis only; the live tier-up→alert path was never verified on real data, and the gate may mean no critical alert has ever posted. Check logs/alerts, and if none, replay one active thesis through the cycle in a dry run. Why better: confirms a user-visible path isn't silently dead. Effort S / risk low. Live-visible: no. Evidence: todo/wolf-macro-brain.md:53; main.py:772-792; wolf_confluence.py:246-251.
- **Test the live freshness-gate branch of the beneficiary digest** — the per-thesis staleness/flag/stage gate that decides which beneficiary rows reach the #news digest has never been asserted against the database, only via a hand-built payload. Add a test seeding fresh + stale rows and asserting only the fresh one is posted. Why better: a regression here silently posts stale or wrong-stage picks to #news. Effort S / risk low. Live-visible: no. Evidence: wolf_digest.py:113-129; test_wolf_beneficiaries.py:26-39; consensus.yaml:808.
- **Add a wrong-side ordering test for the Wolf trade-idea render** — the 17 existing tests never feed a short with entry below target (or a long with entry above target), which is exactly the failure an AI mis-capture produces. Add that test. Why better: it's the highest-risk input and the one case with no coverage; pairs with the ordering-guard fix as its regression lock. Effort S / risk low. Live-visible: no. Evidence: tests/test_wolf_trade_idea.py:34-61,125-128.
- **Add a short/bearish-consensus regression test for the YouTube boost** — the only guard on the YouTube boost uses long direction exclusively; the surprising current behavior (bearish YouTube raises a long score) is untested. Pin the current behavior so the direction-aware fix has a baseline to flip against. Why better: closes a silent-regression gap and is the prerequisite for safely shipping the direction-aware change. Effort S / risk low. Live-visible: no. Evidence: tests/test_yt_score_visibility.py:24.
- **Re-process a small set of recent multi-stock videos so B3 has live data** — after flipping the B3 flag, optionally clear the "already processed" mark for 2-3 recent multi-stock videos so the next poll re-watches them with tagging on, giving an instant before/after. Why better: all 495 existing visual rows are untagged and the skip-check blocks retroactive tagging, so without forcing a re-watch the only evidence is unit tests. Costs a few free-tier Gemini calls. Effort S / risk med. Live-visible: no. Evidence: scanners/youtube.py:713; live database 0 tagged of 495.
- **Verify the `!all` synthesis retry ladder doesn't fire 3-4 full calls on the common path** — synthesis can run up to 4 times (initial + 3 retries), each re-sending the full ~3.6k-token prompt, but how often the free chain trips each retry on real tickers is unmeasured. Quantify the retry rate from the obs logs. Why better: if retries fire often, tightening the first-pass prompt is the cheapest cost win in the area — but it's only worth doing if the rate is actually high. Effort M / risk low. Live-visible: no. Evidence: narrator.py:1051,1073,1101,1116-1125.

### Flag-flips (already built)
- **Flip B3 per-number ticker tagging ON and run a real multi-stock before/after** — turn on `youtube.visual.per_number_ticker_tagging`, restart, let it process the next few multi-stock videos, then run `!all` on two tickers from the same video and compare. With it ON, each ticker's alert should only show numbers tagged to it instead of the top ticker absorbing everyone's chart prices. Why better: today a 7-stock video dumps all its chart numbers onto whichever ticker had the most mentions, so secondary tickers get nothing useful; tagging routes each number to its own stock. Flag-off is byte-identical to before, so the flip is a safe one-session change. Effort S / risk low. Live-visible: yes. Evidence: consensus.yaml:459; gemini_video_parser.py:82; db.py:2199-2223; live video IpqwTKyG4hE 7 tickers/16 rows, 0 tagged.

### Risky refactors (flag-gated)
(The four flag-gated YouTube-score changes — direction-aware, recency decay, channel-reliability, and level confluence — are the gated-refactor work in this audit. They share a load-bearing constraint: the score function is used by BOTH the live alerting engine AND `!all`, so any value or sign change shifts which alerts fire and MUST stay behind a default-OFF flag until blessed. They are listed in full under Optimize-quality above; this header points to them so the gating constraint is visible in one place.)

### Bugfixes
- **Guard Wolf trade-idea entry/target ordering** — the render never checks that entry sits on the correct side of target, so a short with entry below its target ("short $70,000 → $74.2k") can post live to #news. Reject the setup when a short has entry ≤ target or a long has entry ≥ target. Why better: the parser is explicitly non-deterministic and nothing downstream catches a backwards idea; a wrong-side idea is worse than falling back to plain "Key levels." Effort S / risk low. Live-visible: yes. Evidence: wolf_news.py:176,180-181; wolf_email_parser.py:217.
- **Reject non-positive entry/target prices at parse time** — the parser keeps a setup if entry OR target is present and accepts 0 or negatives, so a model emitting entry:0 renders "$0" in #news. Require both to be > 0. Why better: a cheap defensive guard at the validation boundary where every other field is already sanitized. Effort S / risk low. Live-visible: yes. Evidence: wolf_email_parser.py:218-226; wolf_news.py:107.
- **Wire up the dead `shorts_enabled` config flag** — the flag exists with a comment claiming "short buckets exist in the map but are gated off," but no short buckets exist and the flag is never read anywhere — the short side simply isn't built. As part of the v1.1 build, make the code actually read the flag; until then, fix the misleading comment. Why better: removes a false "just flip the flag" signal that could mislead a future session. Effort S / risk low. Live-visible: no. Evidence: consensus.yaml:818; grep finds 0 uses.
- **Extend B3 tag into the structured-levels path so `youtube_levels` stops mis-attributing** — flipping B3 ON only fixes the AI narrative text; the structured levels table (which feeds scoring, cross-reference, and the `!all` support/resistance anchors) still hard-codes the top ticker and never reads the per-number tag. Read the tag in the levels path and file each level under its own ticker. Why better: closes the "all numbers on the top ticker" problem in the pipeline that actually drives the anchors, not just the prose. Effort M / risk med. Live-visible: yes. Evidence: scanners/youtube.py:452; video_classifier.py:859; scanners/youtube.py:303.
- **Update stale gemini-video-eval-assertions.md status (doc-only)** — the todo still reads "OPEN — A1-A3 open," but the harness was rewritten and the visual→levels code now exists and passes 6/6 gating checks in the latest cron. Close the todo or rewrite its "remaining" section to the one real open question (why A2 filed 0 levels from 17 visual rows). Why better: prevents a future session re-chasing an already-fixed gap. Effort S / risk low. Live-visible: no. Evidence: todo/gemini-video-eval-assertions.md:1-3 vs .omc/logs/v2_assertions_20260606.log; video_classifier.py:800-815; scanners/youtube.py:459-462.
- **Reconcile dod-checklist-scope-aware.md (marked DONE) with CLAUDE.md** — the todo says DONE and its acceptance required CLAUDE.md to be restructured into tag-keyed buckets, but CLAUDE.md still has the old flat checklist. Either do the restructure or flip the status back with a note. Why better: a DONE item whose acceptance was never applied hides real undone work and erodes trust in the DONE list. Effort M / risk low. Live-visible: no. Evidence: todo/dod-checklist-scope-aware.md:3,60-64 vs CLAUDE.md:96,100.

### Test-data resources available
- **consensus.db — signal_events** — 2,732 saved source-mention rows (2,709 Twitter, 23 YouTube), 545 in the last 7 days, spanning 2026-04-22 to 2026-06-05. Lets confluence / cross-source / per-ticker scoring builds test against a real 6-week stream. Evidence: count = 2,732; min/max recorded_at 2026-04-22 .. 2026-06-05.
- **consensus.db — options_flow** — 32,794 options rows (strike/volume/OI/premium/spot/vol-OI) from 2026-06-01 to 2026-06-05. The largest table; verifies the options watcher, max-pain, and the `!all` options field, including the load-bearing staleness/after-hours edge cases. Evidence: count = 32,794; min/max detected_at 2026-06-01 .. 2026-06-05.
- **consensus.db — macro_theses IS the Wolf thesis store** — 72 Wolf theses (source='wolf') with full per-update evidence, levels, and trade-setup JSON, 2026-04-30 to 2026-06-01. Canonical Wolf over-time tracker. Brief note: there is NO table named `wolf_theses` — that name resolves to `macro_theses WHERE source='wolf'`. Evidence: count = 72; `SELECT count(*) FROM wolf_theses` → "no such table".
- **consensus.db — youtube_signals + youtube_levels** — 876 signal rows and 321 level rows with confidence scores and suppression flags, 2026-04-23 to 2026-06-05. Tests the caption→AI chain, level alerts, and evidence spans against ~1,200 real rows. Evidence: counts 876 / 321; min/max extracted_at 2026-04-23 .. 2026-06-05.
- **consensus.db — wolf_confluence_checks + wolf_beneficiaries (THIN: one day only)** — 36 and 12 rows, but EVERY row was computed today (2026-06-06) in a single run. Verifies current schema/output shape, but warns: a build needing confluence TRENDS over time has effectively one snapshot. Treat as shape-checks, not trend data. Evidence: both group only to 2026-06-06.
- **Gmail — live token + ~15 unprocessed Wolf emails** — the token auto-refreshed cleanly in a live test; 94 Wolf emails are reachable vs 79 already processed, so ~15 fresh emails are real test fodder, and per-message refetch by Gmail id works. Lets the email parser / vision / digest run against never-seen emails. Evidence: list resultSizeEstimate 94 vs processed 79; wolf_email_excerpt.py:68 refetch path.
- **/tmp/wolf_charts — 112 chart images + OCR text sidecars** — chart JPGs paired with .txt OCR sidecars, generated 2026-05-31. Real inputs for testing the Wolf chart→levels vision pipeline without re-fetching emails. Evidence: ls count = 112; dir mtime 2026-05-31.
- **Discord history fetchable for verification — including #news** — both #chat and #news history can be read back via the Discord REST API, so any build's actual embed output can be verified end-to-end, not just trusting a 200 on send. Two traps for the brief: the working bot token is in `.env.service` (the plain `.env` has a BLANK token), and the call returns 403 unless a `User-Agent` header is sent. Evidence: token len 0 in .env vs 72 in .env.service; no-UA → 403, with-UA → messages; newest #news embed was the live Wolf confluence post.

## Notable token/cost savings
The user specifically wants lower token/API usage. Every cost item, with its estimated saving:

- **Cut the `!all` cleanup phase** — saves ~15-28s wall-clock and ~9 AI calls per `!all`, with zero output loss (the team measured that cleanup output doesn't change the narrative). Biggest single win. (narrator.py:287-289; todo/all-command-quality.md:179)
- **Make the `!all` sanitize phase no-LLM by default** — eliminates up to 7 free-tier AI calls per `!all`, per the maintainer's own note with no quality loss; same phase as above, different angle (flag-flip vs trim). (consensus.yaml:278-285; aggregator.py:1071)
- **Skip the wasted groq head-start on big tickers** — saves up to 15s per big-ticker `!all` on a call that always 413-fails anyway. (llm_client.py:270-273)
- **Disable Gemini "thinking" + cap output on Wolf chart reads** — cuts hidden reasoning tokens per chart read (up to 5 per email) against a tiny ~3-4-call/key/day free quota, directly reducing 429 key rotation. (wolf_vision.py:158-165)
- **Add a wolf_vision budget bucket** — caps and makes-visible the currently-uncapped vision calls that share and can exhaust the video pipeline's free keys. Prevents a cross-feature outage rather than saving raw tokens. (engine.py:55-59; wolf_email_parser.py:357-362)
- **Cache or trim the ~2,800-token static synthesis instruction block** — cuts input tokens on the most expensive call, billed every call × (1 + up to 3 retries). (narrator.py:350-372,577-731)
- **Skip the per-ticker confidence scorer when it can't reach the alert threshold** — removes one AI call (cost + up-to-15s budget) on every low-signal ticker that could never alert. (cross_reference.py:310-319)
- **Drop the two always-empty sanitize batches** — no token win today (they early-return), but removes two no-op futures from every `!all`; listed for completeness. (aggregator.py:1073,1078)
