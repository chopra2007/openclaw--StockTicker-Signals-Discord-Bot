## Handoff: team-plan → team-exec
- **Decided**: Execute the consensus-approved plan at `.omc/plans/2026-04-24-top3-combined-pr-plan.md` on branch `signal-engine-top3-pr` (branched from master @7b7802e). Strict commit order A→H per plan §2.3. No shims, no schema migrations (reuse `decision_snapshots.feature_vector_json`). Atomic 4-site rename for `require_market_confirmation`. Keep `regime_detector.py` on disk.
- **Rejected**: 3-PR sequenced rollout (review overhead); Q1-only first (leaves 78.4% Phase-2 drop live); deprecation shim for rename (violates Principle 2); new `shadow_calibration_log` table (violates Principle 5); deleting `regime_detector.py` (breaks `tests/test_degraded_mode.py:283-367`).
- **Risks**: (1) `source_performance` schema is `rolling_accuracy` not `hit_rate_1h` — use the helper abstraction. (2) `asyncio.wait_for` MUST wrap only `xref_task`, not the full `asyncio.gather`. (3) Both orphan `.pyc` files (`reliability_engine`, `snapshot_builder`) must be deleted; `regime_detector.pyc` stays. (4) 4 files rename atomically: `config/consensus.yaml:297`, `engine.py:234`, `engine.py:294`, `tests/test_engine.py:246`. (5) Commit E must update `main.py:608` call site in the SAME commit as `db.py:672-682` signature change.
- **Files**: `.omc/plans/2026-04-24-top3-combined-pr-plan.md` (authoritative plan), `plans/AUDIT_RESEARCH_2026-04-24.md` (empirical evidence).
- **Remaining**: 8 commits (A tests → B KILL → C Q2b → D Q2a → E M3 → F Q1 → G rename → H docs), then Task 9 verifier runs all 13 ACs.
